# 当前版本备注

日期：2026-07-31

项目：Flow Vul Hunt

当前状态：

- 项目采用前后端分离结构：
  - 前端目录：`frontend/`
  - 后端目录：`backend/`
- 前端开发服务器配置为 `5173` 端口，并将 `/api`、`/health` 代理到后端 `127.0.0.1:8000`。
- 后端提供 `/health` 和 `/api/*` 接口，当前可正常响应。
- 已验证当前环境下前后端联通：
  - `http://127.0.0.1:8000/health` 可返回健康检查结果
  - `http://127.0.0.1:5173/health` 可通过前端代理访问后端
  - `http://127.0.0.1:5173/api/system/metrics` 可返回真实后端数据

附加说明：

- 根目录不能直接执行 `npm run dev`，需要进入 `frontend/` 目录后启动前端。
- 后端版本号当前为 `0.1.0`。
- 前端 `package.json` 版本号当前为 `0.0.0`。
- 健康检查返回中，数据库迁移状态表现为 `current: null`、`expected: 0007`，说明服务可用，但迁移记录仍建议后续核对。
